
k3s_cluster:
  children:
    server:
      hosts:
        homelab: #192.168.87.42:
    agent:
      hosts:
        rpi5: #192.168.87.64:
        rognode: #192.168.87.56:

  # Required Vars
  vars:
    ansible_port: 22
    api_endpoint: "{{ vars[groups['server'][0]]['ansible_host'] | default(groups['server'][0]) }}"
    user_kubectl: true